{
  "name": "unify-examples-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for unify examples",
  "type": "module",
  "scripts": {
    "test": "node test-runner.js",
    "test:verbose": "node test-runner.js --verbose",
    "build-all": "bash build-all.sh",
    "clean-all": "bash clean-all.sh",
    "validate": "bash validate-examples.sh"
  },
  "dependencies": {
    "fs-extra": "^11.1.1",
    "chalk": "^5.3.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
